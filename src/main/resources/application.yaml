server:
  port: 8080
  tomcat:
    basedir: "/"
    
spring:
  application:
    name: jenkins-pipeline-helloworld

management:
  endpoint:
    restart:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  server:
    port: 8081
